<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.core">

    <application
        android:name=".di.CoreApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:noHistory="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity
            android:name=".ui.signin.SigninActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.main.MainActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.profilechange.ProfileChangeActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.selfcall.SelfCallActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.suggestion.SuggestionActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.order.OrderActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.orderlist.OrderListActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.orderlist.order.OrderListOrderActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.orderlist.selfcall.OrderListSelfCallActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.orderlist.suggestion.OrderListSuggestionActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.ownermain.OwnerMainActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.ownermain.alluser.OwnerMainAllUserActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.ownermain.order.OwnerMainOrderActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.ownermain.selfcall.OwnerMainSelfCallActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

        <activity android:name=".ui.ownermain.suggestion.OwnerMainSuggestionActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Core" />

    </application>

</manifest>